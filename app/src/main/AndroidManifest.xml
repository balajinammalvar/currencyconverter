<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="online.balaji.currencyconverter">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CurrencyConverter">
        <activity
            android:name=".view.constraintlayout.ConstraintLayout"
            android:exported="false" />
        <activity
            android:name=".view.scopfunction.ScopeFunctionActivity"
            android:exported="false" />
        <activity
            android:name=".view.flow.FlowActivity"
            android:exported="false" />
        <activity
            android:name=".view.movies.MoviesList"
            android:exported="false" />
        <activity
            android:name="online.balaji.currencyconverter.view.splash.view.home.CurrencyConverter"
            android:exported="true" />
        <activity
            android:name="online.balaji.currencyconverter.view.splash.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>